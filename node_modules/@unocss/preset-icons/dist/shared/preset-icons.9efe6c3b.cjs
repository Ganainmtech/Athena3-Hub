'use strict';

const ofetch = require('ofetch');
const core = require('./preset-icons.a6e5ccac.cjs');

function createCDNLoader(cdnBase) {
  return core.createCDNFetchLoader(ofetch.$fetch, cdnBase);
}

exports.createCDNLoader = createCDNLoader;
